{% set element = (element is defined) ? element : 'h2' %}
{% if block.title is defined and block.title is not empty %}
    <{{ element }} property='title'>{{ block.title }}</{{ element }}>
{% endif %}
